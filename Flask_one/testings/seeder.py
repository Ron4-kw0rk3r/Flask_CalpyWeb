#!/usr/bin/python3


import sys
import math


loader = ['/' , '+', '*', '-', '%']
check_bot = ""
def kworl_pi():
    for ext in ['/' , '+', '*', '-', '%']: 
    
        if ext ==  loader[0]:
            comp = lambda a : a / s
            return  comp(s) 
        elif ext == loader[1]:
            pass

            
def jkwork():
    sed = lambda x : x + w
    return sed(w)


def jkwork():
    sed = lambda x : x - w
    return sed(w)


def jkwork():
    sed = lambda x : x * w
    return sed(w)


def jkwork():
    sed = lambda x : x / w
    return sed(w)

